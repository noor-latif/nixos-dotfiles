# reload config
bind=SUPER+SHIFT,r,reload_config

# terminal and launcher
bind=SUPER+SHIFT,Return,spawn,foot
bind=SUPER,d,spawn,rofi -config ~/.config/mango/rofi/config.rasi -show drun

# kill and quit
bind=SUPER,q,killclient
bind=SUPER,m,quit

# switch window focus (hjkl)
bind=SUPER,j,focusstack,next
bind=SUPER,k,focusstack,prev

# master resize (from argos)
bind=SUPER,h,setmfact,-0.05
bind=SUPER,l,setmfact,+0.05

# zoom to master (from argos)
bind=SUPER,Return,zoom

# inc/dec master count
bind=SUPER,i,incnmaster,+1
bind=SUPER,p,incnmaster,-1

# swap window
bind=SUPER+SHIFT,k,exchange_client,up
bind=SUPER+SHIFT,j,exchange_client,down
bind=SUPER+SHIFT,h,exchange_client,left
bind=SUPER+SHIFT,l,exchange_client,right

# smart move window
bind=CTRL+SHIFT,k,smartmovewin,up
bind=CTRL+SHIFT,j,smartmovewin,down
bind=CTRL+SHIFT,h,smartmovewin,left
bind=CTRL+SHIFT,l,smartmovewin,right

# window status
bind=SUPER,f,togglefloating,
bind=SUPER+SHIFT,f,togglefullscreen,
bind=SUPER,0,toggleoverview
bind=SUPER,a,togglegaps

# layouts
bind=SUPER,t,setlayout,tile
bind=SUPER,v,setlayout,vertical_grid
bind=SUPER,c,setlayout,spiral
bind=SUPER,x,setlayout,scroller
bind=SUPER,n,switch_layout

# workspaces (comboview)
bind=SUPER,1,comboview,1
bind=SUPER,2,comboview,2
bind=SUPER,3,comboview,3
bind=SUPER,4,comboview,4
bind=SUPER,5,comboview,5
bind=SUPER,6,comboview,6
bind=SUPER,7,comboview,7
bind=SUPER,8,comboview,8
bind=SUPER,9,comboview,9

# move window to workspace
bind=SUPER+SHIFT,1,tag,1,0
bind=SUPER+SHIFT,2,tag,2,0
bind=SUPER+SHIFT,3,tag,3,0
bind=SUPER+SHIFT,4,tag,4,0
bind=SUPER+SHIFT,5,tag,5,0
bind=SUPER+SHIFT,6,tag,6,0
bind=SUPER+SHIFT,7,tag,7,0
bind=SUPER+SHIFT,8,tag,8,0
bind=SUPER+SHIFT,9,tag,9,0

# mouse bindings
mousebind=SUPER,btn_left,moveresize,curmove
mousebind=SUPER,btn_right,moveresize,curresize

# gestures
gesturebind=none,left,3,focusdir,left
gesturebind=none,right,3,focusdir,right
gesturebind=none,up,3,focusdir,up
gesturebind=none,down,3,focusdir,down
gesturebind=none,left,4,viewtoleft_have_client
gesturebind=none,right,4,viewtoright_have_client
gesturebind=none,up,4,toggleoverview
gesturebind=none,down,4,toggleoverview

# brightness and volume
bind=none,XF86AudioRaiseVolume,spawn,~/.config/mango/scripts/volume.sh up
bind=none,XF86AudioLowerVolume,spawn,~/.config/mango/scripts/volume.sh down
bind=none,XF86MonBrightnessUp,spawn,~/.config/mango/scripts/brightness.sh up
bind=none,XF86MonBrightnessDown,spawn,~/.config/mango/scripts/brightness.sh down

# custom app binds
bind=SUPER,b,spawn,google-chrome
bind=SUPER+SHIFT,e,spawn,foot -e yazi
bind=SUPER,s,spawn_shell,grim -g "$(slurp -b '#2E2A1E55' -c '#fb751bff')" -t ppm - | satty -f -
bind=SUPER+SHIFT,b,spawn,bash ~/.config/mango/scripts/hide_waybar_mango.sh
bind=SUPER+SHIFT,x,spawn,swaylock -f -i ~/.config/mango/wallpaper/wallpaper-mono.jpeg
bind=CTRL+ALT,backslash,spawn,swaync-client -t
bind=CTRL+ALT,BackSpace,spawn,swaync-client -C
